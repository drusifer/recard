{
  "name": "recard",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:e2e": "node tests/e2e.smoke.mjs",
    "dev": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
